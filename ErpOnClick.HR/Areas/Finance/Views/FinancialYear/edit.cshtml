@using ErpOnClick.DAL.Models
@using ErpOnClick.ErpMain.Resources
@inject LocService SharedLocalizer

@{
    ApplicationParameters ApplicationParameterDetail = Model.ApplicationParameterIdDetail;
    bool isEdit = (ApplicationParameterDetail != null);
}



<form id="frmEdit" role="form">
    <input type="hidden" id="ACTION_TYPE" name="ACTION_TYPE" value="@(isEdit ? "update" : "save")" />
    <input type="hidden" id="ApplicationParameterId" name="ApplicationParameterId" class="form-control" value="@(ApplicationParameterDetail != null ? ApplicationParameterDetail.ApplicationParameterId : 0)" @(isEdit ? "readonly" : "") min="3" maxlength="3">
    <div hidden="hidden">

        <div class="row">

            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Company Name Eng")</label>
                    <input type="text" id="CompanyNameEn" name="CompanyNameEn" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.CompanyNameEn: null)"
                           required maxlength="70">
                </div>
            </div>
            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Company Name Arb")</label>
                    <input type="text" id="CompanyNameEn" name="CompanyNameEn" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.CompanyNameAr: null)"
                           required maxlength="70">
                </div>
            </div>
            <div class="col-sm-2" style="width:80px;height:80px;background-color:red;">
            </div>
        </div>

        <div class="row">

            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Phone1")</label>
                    <input type="text" id="CompanyPhone1" name="CompanyPhone1" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.CompanyPhone1: null)"
                           required maxlength="16">
                </div>
            </div>
            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Phone2")</label>
                    <input type="text" id="CompanyPhone2" name="CompanyPhone2" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.CompanyPhone2: null)"
                           maxlength="16">
                </div>
            </div>
        </div>
        <div class="row">

            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Address Eng")</label>
                    <input type="text" id="CompanyAddressEn" name="CompanyAddressEn" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.CompanyAddressEn: null)"
                           maxlength="500">
                </div>
            </div>
            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Address Arb")</label>
                    <input type="text" id="CompanyAddressAr" name="CompanyAddressAr" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.CompanyAddressAr: null)"
                           maxlength="50">
                </div>
            </div>
        </div>
        <div class="row">

            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Signature Manager 1")</label>
                    <input type="text" id="ReportFooterSignatureManager1" name="ReportFooterSignatureManager1" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.ReportFooterSignatureManager1: null)"
                           required maxlength="90">
                </div>
            </div>
            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Signature Manager 2")</label>
                    <input type="text" id="ReportFooterSignatureManager2" name="ReportFooterSignatureManager2" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.ReportFooterSignatureManager2: null)"
                           maxlength="90">
                </div>
            </div>
        </div>
        <div class="row">

            <div class="col-sm-5">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Website")</label>
                    <input type="text" id="CompanyWebsite" name="CompanyWebsite" class="form-control"
                           value="@(ApplicationParameterDetail!=null?ApplicationParameterDetail.CompanyWebsite: null)"
                           maxlength="90">
                </div>
            </div>

        </div>
        <div class="col-sm-6 d-none">
            <div class="form-group">
                <label>@SharedLocalizer.GetLocalizedHtmlString("Active Status")</label>
                <select id="IsEnable" name="IsEnable" class="form-control">
                    @*<option></option>*@
                    <option value="True" @(ApplicationParameterDetail != null && ApplicationParameterDetail.IsEnable == true ? "selected" : "")>Active</option>
                    <option value="False" @(ApplicationParameterDetail != null && ApplicationParameterDetail.IsEnable == false ? "selected" : "")>Inactive</option>
                </select>
            </div>
        </div>

    </div>
    <div class="card card-default card-outline col-lg-8">

        <div class="card-header">
            <h2 class="card-title">
                @SharedLocalizer.GetLocalizedHtmlString("Financial Year")
            </h2>
            <!-- tools box -->
            <div class="card-tools fa-pull-right">
                <button type="submit" class="btn  btn-sm btn-primary">
                    <i class="fal fa-save fa-fw"></i> @SharedLocalizer.GetLocalizedHtmlString("Save")
                </button>
                <a href="~/Finance/Customers" type="button" class="btn  btn-sm btn-default">
                    <i class="fal fa-arrow-left fa-fw"></i> @SharedLocalizer.GetLocalizedHtmlString("Return")
                </a>
            </div>
            <!-- /. tools -->
        </div>
    </div>
    <div class="card card-default card-outline col-lg-8">
        <div class="card-header">
            <h2 class="card-title">
                @SharedLocalizer.GetLocalizedHtmlString("Financial Year Detail")
            </h2>
        </div>
        <div class="card-body">

            <div class="row">

                <div class="col-sm-6">
                    <div class="form-group">
                        <label>@SharedLocalizer.GetLocalizedHtmlString("Financial Year Start Month")</label>
                        <select id="FinancialYearStartMonth" name="FinancialYearStartMonth" class="form-control select" required>
                            <option value=""></option>
                            @for (int count = 1; count <= 12; count++)
                            {
                                string month = string.Empty;
                                if (count == 1)
                                    month = "Jan";
                                else if (count == 2)
                                    month = "Feb";
                                else if (count == 3)
                                    month = "Mar";
                                else if (count == 4)
                                    month = "Apr";
                                else if (count == 5)
                                    month = "May";
                                else if (count == 6)
                                    month = "Jun";
                                else if (count == 7)
                                    month = "Jul";
                                else if (count == 8)
                                    month = "Aug";
                                else if (count == 9)
                                    month = "Sep";
                                else if (count == 10)
                                    month = "Oct";
                                else if (count == 11)
                                    month = "Nov";
                                else if (count == 12)
                                    month = "Dec";

                                <option value="@count" @(ApplicationParameterDetail != null && count == ApplicationParameterDetail.FinancialYearStartMonth ? "selected" : "")>@month</option>
                            }


                        </select>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="form-group">
                        <label>@SharedLocalizer.GetLocalizedHtmlString("Financial Start Year")</label>
                        <select id="FinancialYearStartDate" name="FinancialYearStartDate" class="form-control select" required>
                            <option value=""></option>
                            @for (int count = 2019; count <= 2021; count++)
                            {
                                <option value="@count" @(ApplicationParameterDetail != null && count == ApplicationParameterDetail.FinancialYearStartDate ? "selected" : "")>@count</option>
                            }

                        </select>
                    </div>
                </div>
            </div>

            <div class="row">

                <div class="col-sm-6">
                    <div class="form-group">
                        <label>@SharedLocalizer.GetLocalizedHtmlString("Financial Year End Month")</label>
                        <select id="FinancialYearEndMonth" name="FinancialYearEndMonth" class="form-control select"  required>
                            <option value=""></option>
                            @for (int count = 1; count <= 12; count++)
                            {
                                string month = string.Empty;
                                if (count == 1)
                                    month = "Jan";
                                else if (count == 2)
                                    month = "Feb";
                                else if (count == 3)
                                    month = "Mar";
                                else if (count == 4)
                                    month = "Apr";
                                else if (count == 5)
                                    month = "May";
                                else if (count == 6)
                                    month = "Jun";
                                else if (count == 7)
                                    month = "Jul";
                                else if (count == 8)
                                    month = "Aug";
                                else if (count == 9)
                                    month = "Sep";
                                else if (count == 10)
                                    month = "Oct";
                                else if (count == 11)
                                    month = "Nov";
                                else if (count == 12)
                                    month = "Dec";

                                <option value="@count" @(ApplicationParameterDetail != null && count == ApplicationParameterDetail.FinancialYearEndMonth ? "selected" : "")>@month</option>
                            }

                        </select>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="form-group">
                        <label>@SharedLocalizer.GetLocalizedHtmlString("Financial End Year")</label>
                        <select id="FinancialYearEndDate" name="FinancialYearEndDate" class="form-control select" required>
                            <option value=""></option>
                            @for (int count = 2019; count <= 2021; count++)
                            {
                                <option value="@count" @(ApplicationParameterDetail != null && count == ApplicationParameterDetail.FinancialYearEndDate ? "selected" : "")>@count</option>
                            }

                        </select>
                    </div>
                </div>
            </div>
            <div class="col-sm-6 d-none">
                <div class="form-group">
                    <label>@SharedLocalizer.GetLocalizedHtmlString("Active Status")</label>
                    <select id="IsEnable" name="IsEnable" class="form-control">
                        @*<option></option>*@
                        <option value="True" @(ApplicationParameterDetail != null && ApplicationParameterDetail.IsEnable == true ? "selected" : "")>Active</option>
                        <option value="False" @(ApplicationParameterDetail != null && ApplicationParameterDetail.IsEnable == false ? "selected" : "")>Inactive</option>
                    </select>
                </div>
            </div>

        </div>
    </div>







</form>





@section scripts{    
    <script>
        $(function () {

        });

        $.validator.setDefaults({
            submitHandler: function (e) {

                submitForm(e);
            }
        });
        $('#frmEdit').validate({
            rules: {
                //PlateNo: {
                //    maxlength: 20
                //},
                //AssetNo: {
                //    maxlength: 20
                //},
                //Note: {
                //    maxlength: 600
                //},
                //VehicleDescription: {
                //    required: true,
                //    maxlength: 100
                //}

            },
            messages: {
                //PlateNo: {
                //    maxlength: "Maximum 20 characters can be enetered"
                //},
                //AssetNo: {
                //    maxlength: "Maximum 20 characters can be enetered"
                //},
                //Note: {
                //    maxlength: "Maximum 600 characters can be enetered"
                //},
                //VehicleDescription: {
                //    required: "Field is required",
                //    maxlength: "Maximum 100 characters can be enetered"
                //}

            },
            errorElement: 'span',
            errorPlacement: function (error, element) {
                error.addClass('invalid-feedback');
                element.closest('.form-group').append(error);
            },
            highlight: function (element, errorClass, validClass) {
                $(element).addClass('is-invalid');
            },
            unhighlight: function (element, errorClass, validClass) {
                $(element).removeClass('is-invalid');
            }
        });
        function submitForm(e) {
            //$('#frmEdit').on('submit', function (e) {
            //    e.preventDefault();
            var actionType = $('#ACTION_TYPE').val();

            confirmAction('Confirm ?', 'Are you sure you want to save changes ?', 'info', 'Yes', 'No').then(function (result) {
                if (result.value == true) {

                    var startMonth = parseInt( $('#FinancialYearStartMonth').val());
                    var startYear = parseInt($('#FinancialYearStartDate').val());
                    var endMonth = parseInt($('#FinancialYearEndMonth').val());
                    var endYear = parseInt($('#FinancialYearEndDate').val());
                    if (startYear > endYear) {
                        showMsg('Invalid', 'financial start year must be less than end year', 'warning');
                        return false;
                    }
                    else if (startYear == endYear) {
                        if (startMonth != 1 || endMonth != 12) {
                            showMsg('Valid', 'financial year period must be 12 months', 'warning');
                            return false;
                        }
                    }
                    else if (startYear < endYear) {
                        var start = (11 - startMonth);
                        debugger;
                        var end = (11 - endMonth);
                        if ((startMonth + endMonth) != 11) {
                            showMsg('Valid', 'financial year period must be 12 months', 'warning');
                            return false;
                        }
                        
                    }                    
                    
                    var params = $('#frmEdit').serialize();
                    $.ajax({
                        url: getApplicationName() + "Finance/FinancialYear/" + actionType,
                        async: false,
                        type: "POST",
                        data: params,
                        success: function (data) {
                            if (data.result.isError != true) {
                                showMsg('Saved !', 'Record has been saved !', 'success');
                                setTimeout(function () {
                                    window.location.reload();// = getApplicationName() + 'Finance/FinancialYear';
                                }, 500);
                            }
                            else if (data.result.msg != '') {
                                showMsg('Save Failed !', data.result.msg, 'error');
                            }
                            else {
                                showMsg('Save Failed !', 'Something went wrong. Try again', 'error');
                            }
                        }
                    });
                }
            });




            //});
        }

    </script>

}